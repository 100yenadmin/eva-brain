# Fork Takeover Checkpoint - 2026-05-04

## Current Position

- Repo: `alice-example/your-agent-fork`
- Local worktree: `/path/to/worktree`
- Branch: `your-agent-branch`
- PR: `#<number>` - `takeover: merge upstream release into your agent fork`
- Last pushed commit before this checkpoint: `<commit-hash>`
- Merge state on GitHub: `CLEAN`
- Review state on GitHub: `CHANGES_REQUESTED`
- GitHub checks on pushed commit: all green, including CodeRabbit success

Important: this checkpoint was created before committing the second review-hardening patch. If this file appears in a later commit, the verification below reflects the state immediately before that commit.

## Already Pushed

Commit `820d8e0` hardened the main blocker classes from the takeover plan:

- Marked HTTP/MCP operation contexts as remote and added remote-security regression coverage.
- Scoped page soft-delete/restore by source identity plus slug.
- Required archived source state before `sources purge`.
- Tightened destructive guard behavior so `--yes` is not a broad data-bearing bypass.
- Bound OAuth code and refresh token consumption to the owning client.
- Made OAuth SQL failures fail closed where schema probing was too broad.
- Made OAuth RLS hardening migration fail loudly when it cannot apply.
- Hardened admin cookies and removed bootstrap token retention from React state.
- Removed duplicate `auth` CLI dispatch.
- Made init post-install advisory best effort.
- Preserved no-provider keyword/backlink ranking when hybrid embedding fails.
- Replayed the media-evidence docs from PR #13 onto the #23 base.

## Second Hardening Patch

The second patch addresses another batch of serious or cheap review items:

- `scripts/check-admin-build.sh`: missing `admin/` now fails instead of silently skipping.
- `scripts/run-unit-shard.sh`: validates `--max-concurrency` value.
- `scripts/check-no-legacy-getconnection.sh`: delegates to a parser-based Bun script.
- `scripts/check-no-legacy-getconnection.mjs`: new comment/string-aware singleton DB guard.
- `src/commands/serve.ts`: validates `--port` and `--token-ttl` instead of falling back silently.
- `src/core/brain-registry.ts`: rejects relative mount paths.
- `src/core/eval-capture-scrub.ts`: adds broader international phone redaction and case-insensitive bearer redaction.
- `src/core/migrate.ts`: fixes v34 invalid-index cleanup so `DROP INDEX CONCURRENTLY` is not inside a `DO` block.
- `src/commands/pages.ts`: fails fast on missing `--older-than` and avoids truncated dry-run purge summaries.
- `src/commands/auth.ts`: requires redirect URIs for authorization-code clients and passes them through.
- `admin/src/App.tsx`: gives sidebar nav anchors real `href`s.
- `admin/src/pages/RequestLog.tsx`: shows an error state instead of "No requests yet" after load failure; swaps a link-like filter control to a button.
- `test/core/cycle.test.ts`: keeps the "old brain missing sources table" regression as a unit-level `executeRaw` failure instead of creating a second PGLite WASM runtime inside an already-large shard.
- Tests updated for registry, scrubber, migration v34, cycle source resolution, and shard argument validation.
- Admin dist was regenerated by `bun run verify`.

## Current Git Status

Modified or new files:

- `admin/dist/index.html`
- `admin/dist/assets/index--xp8jHAO.js` deleted
- `admin/dist/assets/index-c0Y4mJwn.js` new
- `admin/src/App.tsx`
- `admin/src/pages/RequestLog.tsx`
- `scripts/check-admin-build.sh`
- `scripts/check-no-legacy-getconnection.sh`
- `scripts/check-no-legacy-getconnection.mjs` new
- `scripts/run-unit-shard.sh`
- `src/commands/auth.ts`
- `src/commands/pages.ts`
- `src/commands/serve.ts`
- `src/core/brain-registry.ts`
- `src/core/eval-capture-scrub.ts`
- `src/core/migrate.ts`
- `test/brain-registry.serial.test.ts`
- `test/core/cycle.test.ts`
- `test/eval-capture-scrub.test.ts`
- `test/migrate.test.ts`
- `test/scripts/run-unit-shard.test.ts`

## Verification Completed

Fresh passing local checks after the local patch:

- `git diff --check`
- `PATH=/path/to/bun/bin:$PATH bun run verify`
- `bun scripts/check-no-legacy-getconnection.mjs`
- `bun test test/brain-registry.serial.test.ts test/eval-capture-scrub.test.ts test/migrate.test.ts test/scripts/run-unit-shard.test.ts`
  - Result: `124 pass, 0 fail`
- Focused Eva/security/media suite:
  - Command included media extraction, media ingest, media CLI smoke, import-file, Codex extraction client, OAuth, hybrid fallback, HTTP security, registry, scrubber, migration, and shard guard tests.
  - Result: `208 pass, 0 fail`
- Targeted cycle regression suite after removing the extra PGLite runtime:
  - `PATH=/path/to/bun/bin:$PATH bun test test/core/cycle.test.ts --max-concurrency=1`
  - Result: `28 pass, 0 fail`
- Full sharded unit run:
  - `PATH=/path/to/bun/bin:$PATH SHARDS=2 GBRAIN_TEST_MAX_CONCURRENCY=1 GBRAIN_TEST_SHARD_TIMEOUT=900 bun run test -- --shards 2 --max-concurrency 1`
  - Result: `3789 pass, 0 fail`

Known local environment note:

- Plain `bun` may not be on PATH in every shell. Use the local Bun binary or prefix `PATH=/path/to/bun/bin:$PATH`.

## GitHub Review State

After CodeRabbit finished on the pushed commit:

- PR #23 is open.
- `mergeStateStatus`: `CLEAN`
- `reviewDecision`: `CHANGES_REQUESTED`
- GitHub checks: green on pushed commit `820d8e0`
- Thread audit:
  - Total review threads: `107`
  - Unresolved threads: `84`
  - Current unresolved threads: `84`

Some of those current unresolved threads are addressed by the local patch above, but GitHub cannot know that until the patch is committed and pushed.

## What Is Left

1. Commit the local patch, likely:
   - `git add ...`
   - `git commit -m "fix: address review hardening follow-ups"`
2. Push `eva/merge-upstream-v0.26.6`.
3. Wait for GitHub checks and CodeRabbit on the new commit.
4. Re-run the review-thread audit and classify remaining threads:
   - blocker/security/data-loss/custom-overlap
   - docs/cosmetic/follow-up
5. Fix any remaining true blockers.
6. Do not merge #23 until the blocker class is clear and the user approves.

## PR And Issue Plan Still Standing

- Keep #23 as the fork catch-up base.
- Do not merge stale PRs #5, #6, #7, #17, #18 as-is.
- Treat #13 as replayed docs; finish issue #12 after #23 is landed or after docs are independently confirmed.
- Rebuild the host bridge PR on top of the catch-up PR later; do not claim no-key host-managed OAuth extraction until a live host runtime smoke proves it.
- Keep #16 open until the live bridge path is proven.
- Defer #11 as LanceDB/visual retrieval spike.
- Close #4 last with a readiness report.

## Resume Command Suggestions

From `/path/to/worktree`:

```bash
git status --short
git diff --check
PATH=/path/to/bun/bin:$PATH bun run verify
PATH=/path/to/bun/bin:$PATH SHARDS=2 GBRAIN_TEST_MAX_CONCURRENCY=1 GBRAIN_TEST_SHARD_TIMEOUT=900 bun run test -- --shards 2 --max-concurrency 1
```

Then commit, push, and inspect PR #23 again with:

```bash
gh pr view <number> --repo alice-example/your-agent-fork --json reviewDecision,mergeStateStatus,statusCheckRollup
```
